# NEVER commit this. Real credentials and account identifiers live here.
cloud.env

# DVC manages these; Git must not.
data/raw/
data/_local_blob/
.dvc/cache/

# Local tracking store
mlruns/
mlartifacts/
mlflow.db

# Lab 2: copies of the study's tracking store, pulled down from BLOB_URI.
# 114 MB of run directories. `make restore-registry` recreates them.
reports/remote-mlruns*/
reports/registry-upload/

reports/*.json
reports/*.joblib
__pycache__/
*.pyc
.pytest_cache/
.venv/

# Excel lock files — created while the specification workbook is open.
~$*

# ruff cache
.ruff_cache/
